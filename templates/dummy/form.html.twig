{% block body %}
    {% for message in app.flashes('success') %}
        <div class="success">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button>Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
